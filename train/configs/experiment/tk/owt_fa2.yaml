
# @package _global_
defaults:
  - /experiment/owt/gpt2s.yaml
  - override /model/gpt2model: gpt2-medium

datamodule:
  batch_size: 8  # Per GPU

train:
  optimizer:
    lr: 1.5e-4

expt_name: 10-19-attn=fa2-repo=2-n=768
name: ${.expt_name}
